import underworld as uw
from . import meshutils

res2D = 4
# res3D = 4

# mesh2D = uw.mesh.FeMesh_Cartesian(
#     elementRes = (res2D, res2D)
#     )
mesh2D = None
# ICmesh = uw.mesh.FeMesh_Annulus()
# ICmesh = uw.mesh.FeMesh_Annulus(
#     angularExtent=[89.7, 90.3],
#     radialLengths=[99., 100.]
#     )
# mesh2D.meshUtils = meshutils.get_meshUtils(mesh2D)
# mesh3D = uw.mesh.FeMesh_Cartesian(
#     elementRes = (res3D, res3D, res3D),
#     minCoord = (0.0, 0.0, 0.0),
#     maxCoord = (1.0, 1.0, 1.0)
#     )
# mesh3D.meshUtils = meshutils.get_meshUtils(mesh3D)
